// Palette — four-color scheme, each slot carries distinct semantic meaning.
// Keep assignments here; never use the hex literals directly in components.
export const LABEL    = '#3D4A4B'; // Speaker labels (You / Bob) — cool slate-teal
export const STATUS   = '#3F5052'; // Status overlay / spinner — warm slate-teal
export const GREETING = '#FFFEF4'; // Launch greeting block — cream
export const DIM      = '#F5F4F2'; // System/hint text — warm off-white
